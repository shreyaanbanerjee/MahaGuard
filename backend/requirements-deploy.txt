# MahaGuard AI — Render Deployment Requirements
# EasyOCR/PyTorch excluded (too large for free-tier build).
# Scanned PDF pages will be skipped; text-based PDFs fully supported.

# FastAPI & Server
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Database - Supabase
supabase==2.10.0

# Document Processing (lightweight — no PyTorch)
PyMuPDF==1.24.14
Pillow==10.4.0

# AI / LLM
google-generativeai==0.8.3
instructor>=1.4.0
openai>=1.40.0

# Embeddings
numpy==1.26.4

# Utilities
python-dotenv==1.0.1
aiofiles==24.1.0
pydantic>=2.11.0
pydantic-settings>=2.7.0
pydantic-core>=2.33.0
tenacity==9.0.0
